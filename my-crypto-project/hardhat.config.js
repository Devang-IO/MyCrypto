require("@nomicfoundation/hardhat-toolbox");

module.exports = {
  solidity: "0.8.20",
  networks: {
    hardhat: {
      // This is the default, so you can omit this entire 'hardhat' section if you want
    }
  }
};